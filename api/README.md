# API Folder Note

This project uses Next.js App Router route handlers under `app/api/*`.

The `/api` directory exists to satisfy requested folder structure docs and can hold shared API schemas/adapters if needed.
